# 📘 Краткая инструкция по SmartOffice

## Что это?

SmartOffice - это готовая база данных PostgreSQL для управления:
- 👥 Сотрудниками и кадрами
- 📊 Проектами и задачами  
- 💼 Оборудованием
- 💰 Зарплатой
- 📈 KPI

## Быстрый старт (3 шага)

### 1. Установите PostgreSQL

Если ещё не установлен:
- Windows: скачайте с [postgresql.org](https://www.postgresql.org/download/windows/)
- Linux: `sudo apt install postgresql postgresql-contrib`

### 2. Запустите скрипт установки

**Windows:**
```cmd
cd SmartOffice\scripts
deploy.bat
```

**Linux/Mac:**
```bash
cd SmartOffice/scripts
chmod +x deploy.sh
./deploy.sh
```

### 3. Подключитесь к базе

```bash
psql -U postgres -d SmartOffice
```

## Первые запросы

### Посмотреть всех сотрудников
```sql
SELECT * FROM v_employee_info;
```

### Посмотреть зарплаты
```sql
SELECT * FROM v_current_salaries ORDER BY current_salary DESC;
```

### Посмотреть активные проекты
```sql
SELECT name, start_date, status FROM projects WHERE status = 'active';
```

### Посмотреть задачи
```sql
SELECT * FROM v_employee_task_load ORDER BY active_tasks DESC;
```

## Структура файлов

```
SmartOffice/
├── README.md              ← Полная документация
├── QUICKSTART.md          ← Быстрый старт
├── ИНСТРУКЦИЯ.md          ← Этот файл
├── DATABASE_SCHEMA.md     ← Схема базы данных
├── CHANGELOG.md           ← История изменений
└── scripts/               ← SQL скрипты
    ├── deploy.bat         ← Установка (Windows)
    ├── deploy.sh          ← Установка (Linux/Mac)
    ├── 00-08_*.sql        ← Файлы базы данных
    └── 07_useful_queries.sql ← Готовые запросы
```

## Что внутри базы данных?

### Основные таблицы

1. **employees** - сотрудники (ФИО, должность, отдел, зарплата)
2. **departments** - отделы компании
3. **positions** - должности
4. **projects** - проекты
5. **tasks** - задачи по проектам
6. **attendance** - учёт рабочего времени
7. **leave_requests** - отпуска и больничные
8. **assets** - оборудование (компьютеры, телефоны)
9. **salaries** - история окладов
10. **payroll** - расчётные листы

И ещё 15+ таблиц для полного функционала!

### Готовые отчёты (Views)

- `v_employee_info` - информация о сотрудниках
- `v_current_salaries` - текущие зарплаты
- `v_employee_task_load` - загруженность по задачам
- `v_current_leave` - кто в отпуске
- `v_asset_assignments` - кому выдано оборудование
- `v_kpi_summary` - результаты KPI

## Полезные команды

### Посмотреть все таблицы
```sql
\dt
```

### Посмотреть структуру таблицы
```sql
\d employees
```

### Посмотреть все представления
```sql
\dv
```

### Выйти из psql
```sql
\q
```

## Примеры использования

### Добавить нового сотрудника
```sql
INSERT INTO employees (full_name, email, hire_date, department_id, position_id)
VALUES ('Иванов Иван Иванович', 'ivanov@company.ru', '2026-03-01', 2, 5);
```

### Добавить задачу
```sql
INSERT INTO tasks (project_id, title, assigned_to, deadline, priority)
VALUES (1, 'Разработать новую функцию', 5, '2026-04-01', 'high');
```

### Отметить приход на работу
```sql
INSERT INTO attendance (employee_id, work_date, check_in, status)
VALUES (1, CURRENT_DATE, CURRENT_TIMESTAMP, 'present');
```

### Создать заявку на отпуск
```sql
INSERT INTO leave_requests (employee_id, start_date, end_date, leave_type)
VALUES (1, '2026-07-01', '2026-07-14', 'vacation');
```

## Готовые запросы

В файле `scripts/07_useful_queries.sql` есть 50+ готовых запросов:

- 📊 Отчёты по персоналу
- ⏰ Учёт рабочего времени
- 📋 Управление проектами
- 💰 Финансовые отчёты
- 💼 Учёт оборудования
- 🎓 Обучение и развитие
- 📈 KPI и аналитика
- 👔 Рекрутинг

Просто откройте файл и скопируйте нужный запрос!

## Резервное копирование

### Создать бэкап
```bash
pg_dump -U postgres SmartOffice > backup.sql
```

### Восстановить из бэкапа
```bash
psql -U postgres -d SmartOffice < backup.sql
```

## Частые вопросы

### Как изменить пароль пользователя postgres?
```sql
ALTER USER postgres PASSWORD 'новый_пароль';
```

### Как удалить базу данных?
```sql
DROP DATABASE SmartOffice;
```

### Как посмотреть размер базы данных?
```sql
SELECT pg_size_pretty(pg_database_size('SmartOffice'));
```

### Как найти сотрудника по имени?
```sql
SELECT * FROM employees WHERE full_name ILIKE '%иван%';
```

### Как посмотреть все отпуска в марте?
```sql
SELECT * FROM leave_requests 
WHERE start_date >= '2026-03-01' 
AND start_date < '2026-04-01';
```

## Что дальше?

1. ✅ Изучите полную документацию в [README.md](README.md)
2. ✅ Посмотрите схему базы в [DATABASE_SCHEMA.md](DATABASE_SCHEMA.md)
3. ✅ Попробуйте запросы из `scripts/07_useful_queries.sql`
4. ✅ Адаптируйте под свои нужды
5. ✅ Подключите к своему приложению

## Нужна помощь?

- 📧 Email: support@smartoffice.ru
- 💬 Telegram: @smartoffice_support
- 📚 Полная документация: [README.md](README.md)

## Возможности системы

✅ Управление персоналом  
✅ Учёт рабочего времени  
✅ Отпуска и больничные  
✅ Проекты и задачи  
✅ Расчёт зарплаты  
✅ Учёт оборудования  
✅ Обучение и сертификаты  
✅ KPI и оценка эффективности  
✅ Рекрутинг и найм  
✅ Командировки  
✅ Аудит изменений  

## Технологии

- PostgreSQL 12+ (рекомендуется 16+)
- Расширения: pg_trgm, btree_gist
- Триггеры для автоматизации
- Представления для отчётов
- Полнотекстовый поиск

---

**Версия:** 1.0.0  
**Дата:** 28 февраля 2026  

**Удачи в использовании SmartOffice! 🚀**
