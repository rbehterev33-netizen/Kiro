# Полное руководство по расширениям PostgreSQL

## Введение

Расширения — это стандартный способ добавить новую функциональность в PostgreSQL без изменения его ядра. Они могут добавлять новые типы данных, функции, методы индексирования и даже подключать внешние источники данных.

## 🔌 Как подключить расширение в PostgreSQL

### Этап 1: Установка расширения (на уровне сервера)

Способ установки зависит от вашей операционной системы и конкретного расширения.

#### Через пакетный менеджер (Ubuntu/Debian)

Большинство стандартных расширений доступны в виде отдельных пакетов:

```bash
sudo apt update
sudo apt install postgresql-contrib-16
```

> **Примечание:** Пакет `postgresql-contrib` содержит множество дополнительных модулей. Для специализированных расширений, таких как PostGIS, пакет может называться иначе (например, `postgis`).

#### Из исходных кодов

Если расширения нет в репозитории, его можно скомпилировать из исходников:

```bash
make
sudo make install
```

### Этап 2: Активация расширения (в конкретной базе данных)

После установки пакета расширение нужно активировать в той базе данных, где вы планируете его использовать:

```sql
CREATE EXTENSION [IF NOT EXISTS] название_расширения;
```

Ключевое слово `IF NOT EXISTS` подавит ошибку, если расширение уже было активировано ранее.

### Проверка установленных расширений

В psql:
```sql
\dx
```

Или через SQL-запрос:
```sql
SELECT * FROM pg_extension;
```

## 🛠️ Топ-20 полезных расширений PostgreSQL


### Категория 1: Работа с новыми типами данных

#### 1. PostGIS

**Описание:** Лидер среди расширений для работы с географическими объектами. Добавляет поддержку пространственных типов данных (точки, линии, полигоны), индексов (R-Tree/GiST) и функций для геоаналитики.

**Для чего нужно:**
- Геоинформационные системы (ГИС)
- Приложения с картами
- Логистика
- Анализ местоположений

**Установка:**
```bash
sudo apt install postgis postgresql-16-postgis-3
```

**Активация:**
```sql
CREATE EXTENSION postgis;
```

**Пример использования:**
```sql
-- Создание таблицы с геометрией
CREATE TABLE cities (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    location GEOMETRY(Point, 4326)
);

-- Вставка данных
INSERT INTO cities (name, location) 
VALUES ('Москва', ST_GeomFromText('POINT(37.6173 55.7558)', 4326));

-- Поиск городов в радиусе 100 км
SELECT name 
FROM cities 
WHERE ST_DWithin(
    location::geography,
    ST_GeomFromText('POINT(37.6173 55.7558)', 4326)::geography,
    100000
);
```

---

#### 2. pgvector

**Описание:** Добавляет тип данных `vector` и возможности векторного поиска по сходству. Позволяет хранить и искать эмбеддинги прямо в PostgreSQL.

**Для чего нужно:**
- Приложения с искусственным интеллектом (AI)
- Семантический поиск
- Рекомендательные системы
- Работа с большими языковыми моделями (LLM)

**Установка:**
```bash
git clone https://github.com/pgvector/pgvector.git
cd pgvector
make
sudo make install
```

**Активация:**
```sql
CREATE EXTENSION vector;
```

**Пример использования:**
```sql
-- Создание таблицы с векторами
CREATE TABLE documents (
    id SERIAL PRIMARY KEY,
    content TEXT,
    embedding vector(1536)
);

-- Создание индекса для быстрого поиска
CREATE INDEX ON documents USING ivfflat (embedding vector_cosine_ops);

-- Поиск похожих документов
SELECT content 
FROM documents 
ORDER BY embedding <=> '[0.1, 0.2, ...]'::vector 
LIMIT 5;
```

---

#### 3. hstore

**Описание:** Реализует тип данных для хранения пар "ключ-значение" в одном поле.

**Для чего нужно:**
- Хранение разреженных данных
- Мета-информация
- Настройки, где структура может меняться от записи к записи

**Активация:**
```sql
CREATE EXTENSION hstore;
```

**Пример использования:**
```sql
-- Создание таблицы с hstore
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    attributes hstore
);

-- Вставка данных
INSERT INTO products (name, attributes) 
VALUES ('Ноутбук', 'color => "black", ram => "16GB", ssd => "512GB"');

-- Запрос по ключу
SELECT name, attributes->'ram' AS ram 
FROM products 
WHERE attributes->'color' = 'black';
```

---

#### 4. citext

**Описание:** Предоставляет регистронезависимый тип текстовых данных.

**Для чего нужно:**
- Поля, где регистр не важен (email, имена пользователей, никнеймы)
- Упрощает запросы, избавляя от постоянного использования `LOWER()`

**Активация:**
```sql
CREATE EXTENSION citext;
```

**Пример использования:**
```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    email citext UNIQUE,
    username citext
);

-- Вставка
INSERT INTO users (email, username) VALUES ('User@Example.com', 'JohnDoe');

-- Поиск без учета регистра
SELECT * FROM users WHERE email = 'user@example.com';  -- Найдет запись
```

---

#### 5. ltree

**Описание:** Содержит тип данных для представления меток данных, хранящихся в древовидной иерархической структуре.

**Для чего нужно:**
- Работа с деревьями
- Каталоги
- Вложенные разделы навигации

**Активация:**
```sql
CREATE EXTENSION ltree;
```

**Пример использования:**
```sql
CREATE TABLE categories (
    id SERIAL PRIMARY KEY,
    path ltree,
    name VARCHAR(100)
);

INSERT INTO categories (path, name) VALUES
    ('electronics', 'Электроника'),
    ('electronics.computers', 'Компьютеры'),
    ('electronics.computers.laptops', 'Ноутбуки');

-- Поиск всех подкатегорий
SELECT * FROM categories WHERE path <@ 'electronics';

-- Поиск прямых потомков
SELECT * FROM categories WHERE path ~ 'electronics.*{1}';
```

---


### Категория 2: Безопасность и шифрование

#### 6. pgcrypto

**Описание:** Набор криптографических функций для хеширования (SHA, MD5) и шифрования (AES) данных.

**Для чего нужно:**
- Безопасное хранение паролей
- Шифрование конфиденциальных данных
- Создание цифровых подписей на уровне базы данных

**Активация:**
```sql
CREATE EXTENSION pgcrypto;
```

**Пример использования:**
```sql
-- Хеширование пароля
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50),
    password_hash TEXT
);

INSERT INTO users (username, password_hash) 
VALUES ('john', crypt('mypassword', gen_salt('bf')));

-- Проверка пароля
SELECT * FROM users 
WHERE username = 'john' 
AND password_hash = crypt('mypassword', password_hash);

-- Шифрование данных
SELECT pgp_sym_encrypt('Секретные данные', 'ключ_шифрования');

-- Расшифровка
SELECT pgp_sym_decrypt(
    pgp_sym_encrypt('Секретные данные', 'ключ'), 
    'ключ'
);
```

---

#### 7. pgaudit

**Описание:** Предоставляет детальное логирование действий пользователей. Записывает, кто, что и когда делал с данными.

**Для чего нужно:**
- Соответствие строгим стандартам безопасности (PCI-DSS, HIPAA)
- Расследование инцидентов
- Аудит действий пользователей

**Установка:**
```bash
sudo apt install postgresql-16-pgaudit
```

**Активация:**
```sql
CREATE EXTENSION pgaudit;
```

**Настройка в postgresql.conf:**
```
shared_preload_libraries = 'pgaudit'
pgaudit.log = 'write, ddl'
pgaudit.log_catalog = off
```

---

### Категория 3: Администрирование, мониторинг и производительность

#### 8. pg_stat_statements

**Описание:** Собирает и хранит статистику по выполнению всех SQL-запросов (количество вызовов, общее время, количество прочитанных строк).

**Для чего нужно:**
- Поиск медленных и проблемных запросов
- Анализ общей нагрузки на базу данных
- Оптимизация производительности

**Активация:**
```sql
CREATE EXTENSION pg_stat_statements;
```

**Настройка в postgresql.conf:**
```
shared_preload_libraries = 'pg_stat_statements'
pg_stat_statements.track = all
```

**Пример использования:**
```sql
-- Топ-10 самых медленных запросов
SELECT 
    query,
    calls,
    total_exec_time,
    mean_exec_time,
    max_exec_time
FROM pg_stat_statements
ORDER BY mean_exec_time DESC
LIMIT 10;

-- Сброс статистики
SELECT pg_stat_statements_reset();
```

---

#### 9. pg_repack

**Описание:** Позволяет перестраивать таблицы и индексы для удаления "мертвых" данных (bloat) без блокировки записи, в отличие от `VACUUM FULL`.

**Для чего нужно:**
- Борьба с раздуванием таблиц
- Освобождение места на диске без остановки сервиса

**Установка:**
```bash
sudo apt install postgresql-16-repack
```

**Использование (из командной строки):**
```bash
# Реорганизация конкретной таблицы
pg_repack -d mydatabase -t mytable

# Реорганизация всей базы данных
pg_repack -d mydatabase
```

---

#### 10. auto_explain

**Описание:** Автоматически логирует планы выполнения медленных запросов.

**Для чего нужно:**
- Диагностика проблем с производительностью в промышленной среде
- Анализ медленных запросов без ручного подключения

**Настройка в postgresql.conf:**
```
shared_preload_libraries = 'auto_explain'
auto_explain.log_min_duration = 1000  # логировать запросы > 1 сек
auto_explain.log_analyze = true
auto_explain.log_buffers = true
auto_explain.log_timing = true
```

---

#### 11. hypopg

**Описание:** Позволяет создавать "гипотетические" индексы, которые не потребляют ресурсы, но показывают, насколько ускорился бы запрос с таким индексом.

**Для чего нужно:**
- Безопасное "что-если" моделирование
- Оценка эффективности новых индексов перед их реальным созданием

**Установка:**
```bash
git clone https://github.com/HypoPG/hypopg.git
cd hypopg
make
sudo make install
```

**Активация:**
```sql
CREATE EXTENSION hypopg;
```

**Пример использования:**
```sql
-- Создание гипотетического индекса
SELECT hypopg_create_index('CREATE INDEX ON users(email)');

-- Проверка плана запроса с гипотетическим индексом
EXPLAIN SELECT * FROM users WHERE email = 'test@example.com';

-- Удаление всех гипотетических индексов
SELECT hypopg_reset();
```

---

#### 12. pg_cron

**Описание:** Встраивает планировщик задач (как cron) прямо в базу данных для выполнения SQL-команд по расписанию.

**Для чего нужно:**
- Регулярная очистка устаревших данных
- Построение отчетов
- Выполнение периодических заданий по обслуживанию

**Установка:**
```bash
sudo apt install postgresql-16-cron
```

**Настройка в postgresql.conf:**
```
shared_preload_libraries = 'pg_cron'
cron.database_name = 'postgres'
```

**Активация:**
```sql
CREATE EXTENSION pg_cron;
```

**Пример использования:**
```sql
-- Запуск задачи каждый день в 3:00
SELECT cron.schedule('cleanup-old-data', '0 3 * * *', 
    'DELETE FROM logs WHERE created_at < NOW() - INTERVAL ''30 days''');

-- Запуск задачи каждые 5 минут
SELECT cron.schedule('refresh-stats', '*/5 * * * *', 
    'REFRESH MATERIALIZED VIEW stats_summary');

-- Просмотр запланированных задач
SELECT * FROM cron.job;

-- Удаление задачи
SELECT cron.unschedule('cleanup-old-data');
```

---

#### 13. pg_partman

**Описание:** Автоматизирует создание и управление секционированными (партицированными) таблицами.

**Для чего нужно:**
- Упрощение работы с очень большими таблицами
- Разбивка по времени или по диапазону значений

**Установка:**
```bash
sudo apt install postgresql-16-partman
```

**Активация:**
```sql
CREATE EXTENSION pg_partman;
```

**Пример использования:**
```sql
-- Создание партиционированной таблицы
CREATE TABLE logs (
    id BIGSERIAL,
    created_at TIMESTAMPTZ NOT NULL,
    message TEXT
) PARTITION BY RANGE (created_at);

-- Настройка автоматического создания партиций
SELECT partman.create_parent(
    'public.logs',
    'created_at',
    'native',
    'daily'
);

-- Автоматическое обслуживание партиций
SELECT partman.run_maintenance();
```

---


### Категория 4: Интеграция и работа с внешними данными

#### 14. postgres_fdw

**Описание:** Позволяет обращаться к таблицам на другом сервере PostgreSQL так, как если бы они были локальными.

**Для чего нужно:**
- Создание распределенных баз данных
- Шардинг
- Миграция данных
- Объединение данных из разных источников в одном запросе

**Активация:**
```sql
CREATE EXTENSION postgres_fdw;
```

**Пример использования:**
```sql
-- Создание сервера
CREATE SERVER remote_server
    FOREIGN DATA WRAPPER postgres_fdw
    OPTIONS (host 'remote.example.com', port '5432', dbname 'remote_db');

-- Создание маппинга пользователя
CREATE USER MAPPING FOR current_user
    SERVER remote_server
    OPTIONS (user 'remote_user', password 'password');

-- Импорт схемы
IMPORT FOREIGN SCHEMA public
    FROM SERVER remote_server
    INTO public;

-- Или создание конкретной внешней таблицы
CREATE FOREIGN TABLE remote_users (
    id INTEGER,
    name VARCHAR(100),
    email VARCHAR(100)
)
SERVER remote_server
OPTIONS (schema_name 'public', table_name 'users');

-- Использование как обычной таблицы
SELECT * FROM remote_users WHERE email LIKE '%@example.com';
```

---

#### 15. dblink

**Описание:** Выполняет SQL-запросы к другой базе данных PostgreSQL.

**Для чего нужно:**
- Более простой, но менее гибкий аналог postgres_fdw
- Ad-hoc задачи по интеграции данных

**Активация:**
```sql
CREATE EXTENSION dblink;
```

**Пример использования:**
```sql
-- Выполнение запроса к удаленной БД
SELECT * FROM dblink(
    'host=remote.example.com dbname=remote_db user=postgres password=secret',
    'SELECT id, name FROM users'
) AS remote_users(id INTEGER, name VARCHAR);

-- Вставка данных в удаленную БД
SELECT dblink_exec(
    'host=remote.example.com dbname=remote_db user=postgres password=secret',
    'INSERT INTO logs (message) VALUES (''Test message'')'
);
```

---

#### 16. mysql_fdw

**Описание:** Обертка сторонних данных (Foreign Data Wrapper) для подключения к MySQL.

**Для чего нужно:**
- Интеграция данных из MySQL в среду PostgreSQL
- Миграция с MySQL на PostgreSQL

**Установка:**
```bash
git clone https://github.com/EnterpriseDB/mysql_fdw.git
cd mysql_fdw
make USE_PGXS=1
sudo make USE_PGXS=1 install
```

**Активация:**
```sql
CREATE EXTENSION mysql_fdw;
```

**Пример использования:**
```sql
-- Создание сервера MySQL
CREATE SERVER mysql_server
    FOREIGN DATA WRAPPER mysql_fdw
    OPTIONS (host 'mysql.example.com', port '3306');

-- Маппинг пользователя
CREATE USER MAPPING FOR current_user
    SERVER mysql_server
    OPTIONS (username 'mysql_user', password 'mysql_password');

-- Создание внешней таблицы
CREATE FOREIGN TABLE mysql_products (
    id INTEGER,
    name VARCHAR(100),
    price DECIMAL(10,2)
)
SERVER mysql_server
OPTIONS (dbname 'shop', table_name 'products');

-- Запрос к MySQL из PostgreSQL
SELECT * FROM mysql_products WHERE price > 100;
```

---

### Категория 5: Поиск и работа с текстом

#### 17. pg_trgm

**Описание:** Реализует поиск по сходству строк на основе триграмм. Позволяет создавать индексы для ускорения `LIKE`, `ILIKE` и %-запросов.

**Для чего нужно:**
- Нечеткий поиск
- Автодополнение (autocomplete)
- Исправление опечаток
- Механизмы поиска "похожих" записей

**Активация:**
```sql
CREATE EXTENSION pg_trgm;
```

**Пример использования:**
```sql
-- Создание индекса для быстрого LIKE-поиска
CREATE INDEX idx_products_name_trgm ON products USING gin (name gin_trgm_ops);

-- Поиск с LIKE теперь использует индекс
SELECT * FROM products WHERE name ILIKE '%ноутбук%';

-- Поиск похожих строк
SELECT name, similarity(name, 'ноутбук') AS sim
FROM products
WHERE similarity(name, 'ноутбук') > 0.3
ORDER BY sim DESC;

-- Поиск с учетом расстояния
SELECT * FROM products 
ORDER BY name <-> 'ноутбук' 
LIMIT 10;
```

---

#### 18. jsquery

**Описание:** Добавляет эффективный язык запросов для данных типа `jsonb`, значительно расширяя возможности поиска по вложенным JSON-структурам.

**Для чего нужно:**
- Сложные запросы к JSON-документам
- Полнотекстовый поиск внутри JSON

**Установка:**
```bash
git clone https://github.com/postgrespro/jsquery.git
cd jsquery
make USE_PGXS=1
sudo make USE_PGXS=1 install
```

**Активация:**
```sql
CREATE EXTENSION jsquery;
```

**Пример использования:**
```sql
CREATE TABLE documents (
    id SERIAL PRIMARY KEY,
    data jsonb
);

-- Создание индекса
CREATE INDEX ON documents USING gin (data jsonb_path_ops);

-- Поиск с jsquery
SELECT * FROM documents 
WHERE data @@ 'user.age > 18 AND user.city = "Moscow"'::jsquery;
```

---

#### 19. unaccent

**Описание:** Предоставляет словарь для удаления диакритических знаков (ударений) из строк.

**Для чего нужно:**
- Упрощение поиска, чтобы запрос "cafe" находил и "café"
- Нормализация текста

**Активация:**
```sql
CREATE EXTENSION unaccent;
```

**Пример использования:**
```sql
-- Удаление акцентов
SELECT unaccent('café'); -- вернет 'cafe'
SELECT unaccent('Москва'); -- вернет 'Москва' (работает с латиницей)

-- Создание индекса без акцентов
CREATE INDEX idx_products_name_unaccent 
ON products (unaccent(name));

-- Поиск без учета акцентов
SELECT * FROM products 
WHERE unaccent(name) ILIKE unaccent('%café%');
```

---

#### 20. fuzzystrmatch

**Описание:** Содержит функции для нечеткого сравнения строк (например, расстояние Левенштейна, Soundex).

**Для чего нужно:**
- Очистка и дедупликация данных
- Нечеткое сравнение имен
- Поиск по звучанию

**Активация:**
```sql
CREATE EXTENSION fuzzystrmatch;
```

**Пример использования:**
```sql
-- Расстояние Левенштейна (количество изменений)
SELECT levenshtein('kitten', 'sitting'); -- вернет 3

-- Soundex (поиск по звучанию)
SELECT soundex('Smith'), soundex('Smyth'); -- вернет одинаковые коды

-- Поиск похожих имен
SELECT name 
FROM users 
WHERE levenshtein(lower(name), lower('John')) <= 2;

-- Metaphone (улучшенный Soundex)
SELECT metaphone('PostgreSQL', 10);
```

---

### Категория 6: Утилиты и расширение индексов

#### 21. uuid-ossp

**Описание:** Генерирует универсальные уникальные идентификаторы (UUID) по разным стандартам (версии 1, 3, 4, 5).

**Для чего нужно:**
- Создание глобально уникальных первичных ключей
- Распределенные системы

**Активация:**
```sql
CREATE EXTENSION "uuid-ossp";
```

**Пример использования:**
```sql
-- Генерация UUID v4 (случайный)
SELECT uuid_generate_v4();

-- Создание таблицы с UUID
CREATE TABLE orders (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    customer_id UUID,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Вставка данных
INSERT INTO orders (customer_id) VALUES (uuid_generate_v4());
```

---

#### 22. btree_gist

**Описание:** Позволяет создавать GiST-индексы на обычных типах данных (числа, даты, текст). Это дает возможность использовать операторы выхода за пределы простого сравнения.

**Для чего нужно:**
- Поиск ближайших соседей
- Сложные запросы с исключением
- Комбинированные индексы

**Активация:**
```sql
CREATE EXTENSION btree_gist;
```

**Пример использования:**
```sql
-- Таблица бронирований
CREATE TABLE bookings (
    id SERIAL PRIMARY KEY,
    room_id INTEGER,
    period tsrange
);

-- Индекс для проверки пересечений
CREATE INDEX ON bookings USING gist (room_id, period);

-- Ограничение: запрет пересекающихся бронирований
ALTER TABLE bookings 
ADD CONSTRAINT no_overlap 
EXCLUDE USING gist (room_id WITH =, period WITH &&);

-- Попытка вставить пересекающееся бронирование вызовет ошибку
INSERT INTO bookings (room_id, period) 
VALUES (1, '[2024-01-01, 2024-01-05)');
```

---

## 📊 Сравнительная таблица расширений

| Расширение | Категория | Сложность установки | Популярность |
|------------|-----------|---------------------|--------------|
| PostGIS | Геоданные | Средняя | ⭐⭐⭐⭐⭐ |
| pgvector | AI/ML | Средняя | ⭐⭐⭐⭐⭐ |
| pg_stat_statements | Мониторинг | Простая | ⭐⭐⭐⭐⭐ |
| pg_trgm | Поиск | Простая | ⭐⭐⭐⭐⭐ |
| pgcrypto | Безопасность | Простая | ⭐⭐⭐⭐ |
| hstore | Типы данных | Простая | ⭐⭐⭐⭐ |
| uuid-ossp | Утилиты | Простая | ⭐⭐⭐⭐ |
| postgres_fdw | Интеграция | Средняя | ⭐⭐⭐⭐ |
| pg_cron | Автоматизация | Средняя | ⭐⭐⭐ |
| citext | Типы данных | Простая | ⭐⭐⭐ |

---

## 🎯 Рекомендации по выбору расширений

### Для веб-приложений
- `pg_trgm` — поиск и автодополнение
- `uuid-ossp` — генерация уникальных ID
- `pgcrypto` — безопасное хранение паролей
- `citext` — работа с email и никнеймами

### Для аналитики и BI
- `pg_stat_statements` — анализ производительности
- `pg_cron` — автоматизация отчетов
- `postgres_fdw` — интеграция данных из разных источников

### Для AI/ML приложений
- `pgvector` — векторный поиск и эмбеддинги
- `pg_trgm` — нечеткий поиск

### Для геосервисов
- `PostGIS` — обязательное расширение для работы с картами

### Для высоконагруженных систем
- `pg_stat_statements` — мониторинг запросов
- `pg_repack` — борьба с bloat
- `pg_partman` — партиционирование больших таблиц

---

## 🔧 Полезные команды

```sql
-- Список всех доступных расширений
SELECT * FROM pg_available_extensions ORDER BY name;

-- Список установленных расширений
SELECT * FROM pg_extension;

-- Информация о конкретном расширении
\dx+ название_расширения

-- Удаление расширения
DROP EXTENSION название_расширения CASCADE;

-- Обновление расширения
ALTER EXTENSION название_расширения UPDATE;

-- Проверка версии расширения
SELECT extname, extversion FROM pg_extension WHERE extname = 'название';
```

---

## 📚 Дополнительные ресурсы

- [Официальная документация PostgreSQL](https://www.postgresql.org/docs/)
- [PostgreSQL Extension Network (PGXN)](https://pgxn.org/)
- [Awesome PostgreSQL](https://github.com/dhamaniasad/awesome-postgres)
- [PostGIS Documentation](https://postgis.net/documentation/)
- [pgvector GitHub](https://github.com/pgvector/pgvector)

---

## ⚠️ Важные замечания

1. Всегда тестируйте расширения в dev-окружении перед установкой в production
2. Некоторые расширения требуют перезапуска PostgreSQL (особенно те, что используют `shared_preload_libraries`)
3. Проверяйте совместимость расширений с вашей версией PostgreSQL
4. Регулярно обновляйте расширения вместе с обновлением PostgreSQL
5. Делайте резервные копии перед установкой новых расширений

---

**Автор:** Обучающий материал по PostgreSQL  
**Дата:** 2026  
**Версия:** 1.0
